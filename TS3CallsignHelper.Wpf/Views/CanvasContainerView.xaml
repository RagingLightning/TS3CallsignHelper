<UserControl x:Class="TS3CallsignHelper.Wpf.Views.CanvasContainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lb="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:vms="clr-namespace:TS3CallsignHelper.Wpf.ViewModels"
             xmlns:views="clr-namespace:TS3CallsignHelper.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="borderBtn" TargetType="Button">
            <Setter Property="Background" Value="DarkGreen"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontWeight" Value="DemiBold"/>
        </Style>
        <Style x:Key="borderTxt" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <DataTemplate DataType="{x:Type vms:CallsignInformationViewModel}">
            <views:CallsignInformationView/>
        </DataTemplate>
    </UserControl.Resources>
    <Grid Width="{Binding Width}" Height="{Binding Height}">
        <lb:Interaction.Behaviors>
            <lb:MouseDragElementBehavior/>
        </lb:Interaction.Behaviors>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <ContentControl Grid.Row="1" Grid.Column="0" Grid.RowSpan="2" Grid.ColumnSpan="3" Content="{Binding CurrentViewModel}">
            
        </ContentControl>
        <DockPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Background="DarkGreen">
            <Button DockPanel.Dock="Right" Width="25" Height="25" Content="X" Style="{StaticResource borderBtn}" Command="{Binding CloseCommand}"/>
            <TextBlock DockPanel.Dock="Left" Height="25" FontSize="15" Text="{Binding ViewName}" Style="{StaticResource borderTxt}"/>
        </DockPanel>
        <Button x:Name="resizeButton" Grid.Row="2" Grid.Column="2" Width="25" Height="25" Style="{StaticResource borderBtn}">
            <lb:Interaction.Triggers>
                <lb:EventTrigger EventName="PreviewMouseDown" SourceObject="{Binding ElementName=resizeButton}">
                    <lb:CallMethodAction TargetObject="{Binding ResizeCommand}" MethodName="Start"/>
                </lb:EventTrigger>
                <lb:EventTrigger EventName="PreviewMouseMove" SourceObject="{Binding ElementName=resizeButton}">
                    <lb:CallMethodAction TargetObject="{Binding ResizeCommand}" MethodName="Step"/>
                </lb:EventTrigger>
                <lb:EventTrigger EventName="PreviewMouseUp" SourceObject="{Binding ElementName=resizeButton}">
                    <lb:CallMethodAction TargetObject="{Binding ResizeCommand}" MethodName="Stop"/>
                </lb:EventTrigger>
            </lb:Interaction.Triggers>
        </Button>
    </Grid>
</UserControl>
